import type { NextConfig } from "next"

// next-intl plugin is webpack-only; vinext uses vite/rolldown.
// The "next-intl/config" alias is wired in vite.config.ts instead.
const nextConfig: NextConfig = {}

export default nextConfig
